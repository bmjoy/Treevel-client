# c#のファイルとして認識する
mode=cs
# linuxスタイルにする
# class, namespaces, function のブレースは下
# if, for, while, enum など他全てのブレースは右
style=linux
# インデントにタブを使う
indent=spaces=4
# namespace文の中をインデントする
indent-namespaces
# switch文の中をインデントする
indent-switches
# case文の中をインデントする
indent-cases
# class文をインデントする
indent-classes
# プリプロセッサをソースコード内のインデントと合わせる
indent-preproc-cond
# 括弧の両側のスペースを削除
unpad-paren
# if, while, switchの後にpaddingを入れる
pad-header
# 演算子の前後にpaddingを入れる
pad-oper
# カンマの後ろにスペースを入れる
pad-comma
# originalファイルを生成しない
suffix=none
# コメントもインデントする
indent-col1-comments
# ラインエンドをLFにする
lineend=linux

attach-return-type
attach-return-type-decl
indent-after-parens
